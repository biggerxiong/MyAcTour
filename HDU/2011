#include<stdio.h>
int main()
{
	int cases, m;
	float sum;
	int t, n, i;
	
	scanf("%d", &m );
	for (cases = 1 ; cases <= m
	; cases ++)
	{
		scanf("%d", &n);
		t = 1; sum=0.0;
		for (i = 1 ; i <= n ; 
		     i ++)
		{
			sum += t *1.0 / i;
			t = -t ;
		}
		printf("%.2f\n", sum );
	}
}